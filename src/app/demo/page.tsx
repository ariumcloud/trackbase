"use client";

import Link from "next/link";
import { ArrowRight, BarChart3, ChevronDown, CircleDollarSign, LayoutDashboard, Link2, Menu, MousePointer2, Sparkles, ShoppingBag, X } from "lucide-react";
import { useState } from "react";

const periods = { "7 dias": { spend: "US$ 42,80", revenue: "US$ 136,40", profit: "US$ 81,20", purchases: "12", views: "1.284", checkouts: "48" }, "30 dias": { spend: "US$ 168,20", revenue: "US$ 621,90", profit: "US$ 387,40", purchases: "49", views: "5.892", checkouts: "191" } };

export default function DemoPage() {
  const [period, setPeriod] = useState<keyof typeof periods>("7 dias");
  const [tab, setTab] = useState("Visão geral");
  const [menu, setMenu] = useState(false);
  const data = periods[period];
  const tabs = ["Visão geral", "Minhas ofertas", "Links e UTMs", "Campanhas", "Integrações e Pixels", "Alertas"];
  return <main className="demo-page"><header className="demo-top"><Link href="/" className="brand"><span className="brand-icon">u↗</span> UTM<span>Liso</span><span className="brand-dot" /></Link><span className="demo-label"><span /> DEMONSTRAÇÃO COM DADOS FICTÍCIOS</span><div className="demo-top-actions"><Link href="/login" className="button small">Criar meu workspace <ArrowRight size={14} /></Link><button className="mobile-demo-menu" onClick={() => setMenu(!menu)} aria-label="Abrir menu">{menu ? <X /> : <Menu />}</button></div></header><div className="demo-shell"><aside className={`demo-sidebar ${menu ? "open" : ""}`}><div className="demo-side-title">SUA OPERAÇÃO</div>{tabs.map((item, index) => <button key={item} className={tab === item ? "active" : ""} onClick={() => { setTab(item); setMenu(false); }}>{index === 0 ? <LayoutDashboard /> : index === 1 ? <ShoppingBag /> : index === 2 ? <Link2 /> : index === 3 ? <BarChart3 /> : index === 4 ? <CircleDollarSign /> : <Sparkles />}{item}</button>)}<div className="demo-side-bottom"><strong>Gostou do que viu?</strong><span>Use seus próprios dados no workspace.</span><Link href="/login" className="button primary">Começar agora <ArrowRight size={14} /></Link></div></aside><section className="demo-content"><div className="demo-breadcrumb">Demo <span>/</span> <strong>{tab}</strong></div><div className="demo-heading"><div><span className="eyebrow">CONTROLE NA MÃO. PAZ NO BOLSO.</span><h1>{tab === "Visão geral" ? "Sua operação, sem achismo." : tab}</h1><p>{tab === "Visão geral" ? "Do clique à venda. Tudo o que importa, em um só lugar." : "Esta é uma prévia do que você poderá acompanhar no UTMLiso."}</p></div><Link href="/login" className="button primary">Criar link UTM <ArrowRight size={15} /></Link></div>{tab === "Visão geral" ? <><div className="demo-toolbar"><div className="demo-selects"><button className={`demo-select ${period === "7 dias" ? "chosen" : ""}`} onClick={() => setPeriod("7 dias")}>7 dias <ChevronDown size={15} /></button><button className={`demo-select ${period === "30 dias" ? "chosen" : ""}`} onClick={() => setPeriod("30 dias")}>30 dias <ChevronDown size={15} /></button><button className="demo-select">Todas as ofertas <ChevronDown size={15} /></button></div><span className="demo-currency">USD · America/Sao_Paulo</span></div><div className="demo-metrics"><Metric title="Investimento" value={data.spend} detail="Meta Ads em USD" tone="yellow" /><Metric title="Faturamento" value={data.revenue} detail="12 compras aprovadas" tone="green" /><Metric title="Lucro operacional" value={data.profit} detail="Receita líquida − investimento" tone="lime" /><Metric title="Compras" value={data.purchases} detail="Pagamentos aprovados" tone="blue" /></div><div className="demo-lower"><section className="demo-panel"><div className="demo-panel-head"><div><h2>Funil da operação</h2><p>Eventos reais da sua landing e checkout.</p></div><span>Rastreamento ponta a ponta</span></div><div className="demo-funnel-row"><span>Visitas</span><b>{data.views}</b><i style={{ width: "100%" }} className="blue" /></div><div className="demo-funnel-row"><span>Cliques em CTA</span><b>312</b><i style={{ width: "57%" }} className="yellow" /></div><div className="demo-funnel-row"><span>Checkouts</span><b>{data.checkouts}</b><i style={{ width: "29%" }} className="orange" /></div><div className="demo-funnel-row"><span>Compras</span><b>{data.purchases}</b><i style={{ width: "14%" }} className="green" /></div></section><section className="demo-panel"><div className="demo-panel-head"><div><h2>Evolução diária</h2><p>Veja onde o dinheiro está indo.</p></div><BarChart3 size={18} /></div><div className="demo-chart"><span className="chart-value">US$ 12,40</span>{[26, 42, 35, 60, 48, 78, 58, 92, 65, 83, 71, 100].map((height, index) => <i key={index} style={{ height: `${height}%` }} />)}</div><div className="chart-axis"><span>01/09</span><span>Hoje</span></div></section></div><div className="demo-callout"><MousePointer2 size={18} /><div><strong>Quer enxergar seus próprios números?</strong><p>Conecte Meta Ads e sua plataforma de checkout em poucos minutos.</p></div><Link href="/login">Criar workspace <ArrowRight size={15} /></Link></div></> : <div className="demo-placeholder"><Sparkles size={26} /><h2>{tab} no seu workspace</h2><p>Na conta real, esta área mostra os dados, filtros e ações da sua operação. Crie seu workspace para começar.</p><Link href="/login" className="button primary">Criar workspace <ArrowRight size={15} /></Link></div>}</section></div></main>;
}

function Metric({ title, value, detail, tone }: { title: string; value: string; detail: string; tone: string }) { return <article className={`demo-metric ${tone}`}><span>{title}</span><strong>{value}</strong><small>{detail}</small></article>; }
